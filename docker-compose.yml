services:
  fao56-mcp:
    build: .
    container_name: fao56-mcp
    ports:
      - "8003:8003"
    environment:
      FAO56_DB: /app/fao56.db
      FASTMCP_HOST: "0.0.0.0"
      FASTMCP_PORT: "8003"
      FASTMCP_MOUNT_PATH: /fao-56
    restart: unless-stopped
