services:
  fao56-mcp:
    build: .
    container_name: fao56-mcp
    ports:
      - "8003:8003"
    environment:
      FAO56_DB: /app/fao56.db
      MCP_HOST: "0.0.0.0"
      MCP_PORT: "8003"
    restart: unless-stopped
